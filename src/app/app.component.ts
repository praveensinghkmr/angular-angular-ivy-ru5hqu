import { Component, VERSION } from '@angular/core';

@Component({
  selector: 'app', // becomes the custom HTML tag
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css'],
})
export class AppComponent {}
